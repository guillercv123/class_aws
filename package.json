{

	"name": "@orderflow/identity-service",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"build": "esbuild src/handlers/*.ts --bundle --platform=node --target=node22 --outdir=dist --external:@aws-sdk/*",
		"deploy": "serverless deploy",
		"remove": "serverless remove",
		"test": "jest",
		"lint": "eslint src --max-warnings 0"
	},
	"dependencies": {
		"@aws-lambda-powertools/logger": "^2.0.0",
		"@aws-sdk/client-cognito-identity-provider": "^3.600.0",
		"aws-jwt-verify": "^5.0.0"
	},

	"devDependencies": {
		"@types/aws-lambda": "^8.10.140",
		"@types/jest": "^29.5.12",
		"esbuild": "^0.23.0",
		"jest": "^29.7.0",
		"serverless": "^4.0.0",
		"ts-jest": "^29.1.5",
		"typescript": "^5.5.0"
	}
}
